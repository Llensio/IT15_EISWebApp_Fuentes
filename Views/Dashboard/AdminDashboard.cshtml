@model SalesDashboardVM

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Dashboard";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<div class="container mt-4">
    <h2 class="mb-4">KPI Dashboard</h2>

    <!-- KPI Cards -->
    <div class="row mb-4">
        <div class="col-md-3">
            <div class="card text-white bg-primary mb-3">
                <div class="card-body">
                    <h5 class="card-title">Active Products</h5>
                    <p class="card-text fs-4">@Model.ActiveProducts</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-success mb-3">
                <div class="card-body">
                    <h5 class="card-title">Total Revenue</h5>
                    <p class="card-text fs-4">₱@Model.TotalRevenue.ToString("N2")</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-warning mb-3">
                <div class="card-body">
                    <h5 class="card-title">Total Units Sold</h5>
                    <p class="card-text fs-4">@Model.TotalUnitsSold</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-danger mb-3">
                <div class="card-body">
                    <h5 class="card-title">Top Product</h5>
                    <p class="card-text fs-4">@Model.TopProduct</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Charts -->
    <div class="row">
        <div class="col-md-6">
            <div class="card mb-4 shadow">
                <div class="card-header">Sales by Product</div>
                <div class="card-body">
                    <canvas id="salesByProductChart"></canvas>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card mb-4 shadow">
                <div class="card-header">Monthly Revenue Trend</div>
                <div class="card-body">
                    <canvas id="monthlyRevenueChart"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // Sales by Product Chart
    var salesByProductCtx = document.getElementById('salesByProductChart').getContext('2d');
    new Chart(salesByProductCtx, {
        type: 'bar',
        data: {
            labels: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.SalesByProduct.Select(s => s.ProductName))),
            datasets: [{
                label: 'Revenue',
                data: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.SalesByProduct.Select(s => s.TotalRevenue))),
                backgroundColor: 'rgba(54, 162, 235, 0.6)',
                borderColor: 'rgba(54, 162, 235, 1)',
                borderWidth: 1
            }]
        },
        options: { responsive: true, scales: { y: { beginAtZero: true } } }
    });

    // Monthly Revenue Chart
    var monthlyRevenueCtx = document.getElementById('monthlyRevenueChart').getContext('2d');
    new Chart(monthlyRevenueCtx, {
        type: 'line',
        data: {
            labels: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.MonthlyTrends.Select(m => m.Month))),
            datasets: [{
                label: 'Revenue',
                data: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.MonthlyTrends.Select(m => m.Revenue))),
                backgroundColor: 'rgba(255, 99, 132, 0.2)',
                borderColor: 'rgba(255, 99, 132, 1)',
                fill: true,
                tension: 0.3
            }]
        },
        options: { responsive: true, scales: { y: { beginAtZero: true } } }
    });
</script>
@* @model SalesDashboardVM

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Analytics Hub";
}

<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

<style>
    :root {
        --glass-bg: rgba(255, 255, 255, 0.9);
        --accent-primary: #6366f1;
        --accent-success: #10b981;
        --accent-warning: #f59e0b;
        --accent-danger: #ef4444;
        --text-main: #1e293b;
        --text-muted: #64748b;
    }

    body {
        background-color: #f1f5f9;
        font-family: 'Plus Jakarta Sans', sans-serif;
        color: var(--text-main);
    }

    /* Dashboard Wrapper */
    .dashboard-wrapper {
        padding: 1.5rem;
    }

    /* Glass Cards */
    .glass-card {
        background: var(--glass-bg);
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: 20px;
        backdrop-filter: blur(10px);
        box-shadow: 0 4px 20px -5px rgba(0, 0, 0, 0.05);
        transition: all 0.3s ease;
    }

    .kpi-stat-card {
        padding: 1.5rem;
        position: relative;
        overflow: hidden;
    }

        .kpi-stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 25px -5px rgba(0, 0, 0, 0.1);
        }

    /* Icon Styling */
    .icon-shape {
        width: 54px;
        height: 54px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.4rem;
        margin-bottom: 1.25rem;
    }

    .stat-label {
        color: var(--text-muted);
        font-weight: 600;
        font-size: 0.85rem;
        letter-spacing: 0.025em;
    }

    .stat-value {
        font-size: 1.85rem;
        font-weight: 800;
        margin: 0;
    }

    /* Header Customization */
    .page-header {
        margin-bottom: 2rem;
    }

        .page-header h2 {
            font-weight: 800;
            letter-spacing: -0.02em;
        }

    /* Chart Containers */
    .chart-container {
        padding: 1.5rem;
        height: 350px;
    }

    .card-title-premium {
        font-weight: 700;
        font-size: 1.1rem;
        padding-bottom: 1rem;
        border-bottom: 1px solid #f1f5f9;
        margin-bottom: 1.5rem;
    }

    /* Trending Indicators */
    .trend-badge {
        font-size: 0.75rem;
        padding: 4px 8px;
        border-radius: 8px;
        font-weight: 600;
    }
</style>

<div class="dashboard-wrapper">
    <div class="page-header d-md-flex align-items-center justify-content-between">
        <div>
            <h2 class="mb-1 text-dark">Business Overview</h2>
            <p class="text-muted small mb-0">Real-time data for your sales performance.</p>
        </div>
        <div class="mt-3 mt-md-0">
            <button class="btn btn-white glass-card border-0 px-4 py-2 me-2">
                <i class="bi bi-calendar3 me-2 text-primary"></i> Last 30 Days
            </button>
            <button class="btn btn-primary shadow-sm border-0 px-4 py-2" style="background: var(--accent-primary); border-radius: 12px;">
                <i class="bi bi-plus-lg me-1"></i> New Entry
            </button>
        </div>
    </div>

    <div class="row g-4 mb-5">
        <div class="col-xl-3 col-sm-6">
            <div class="glass-card kpi-stat-card border-start border-4 border-primary">
                <div class="icon-shape bg-primary bg-opacity-10 text-primary">
                    <i class="bi bi-layers-half"></i>
                </div>
                <p class="stat-label mb-1"><span class="trend-badge bg-success bg-opacity-10 text-success">ACTIVE PRODUCTS</span></p>
                <div class="d-flex align-items-center justify-content-between">
                    <h3 class="stat-value">@Model.ActiveProducts</h3>
                </div>
            </div>
        </div>

        <div class="col-xl-3 col-sm-6">
            <div class="glass-card kpi-stat-card border-start border-4 border-success">
                <div class="icon-shape bg-success bg-opacity-10 text-success">
                    <i class="bi bi-wallet2"></i>
                </div>
                <p class="stat-label mb-1">TOTAL REVENUE</p>
                <h3 class="stat-value">₱@Model.TotalRevenue.ToString("N0")</h3>
            </div>
        </div>

        <div class="col-xl-3 col-sm-6">
            <div class="glass-card kpi-stat-card border-start border-4 border-warning">
                <div class="icon-shape bg-warning bg-opacity-10 text-warning">
                    <i class="bi bi-lightning-charge"></i>
                </div>
                <p class="stat-label mb-1">UNITS DISPATCHED</p>
                <h3 class="stat-value">@Model.TotalUnitsSold.ToString("N0")</h3>
            </div>
        </div>

        <div class="col-xl-3 col-sm-6">
            <div class="glass-card kpi-stat-card border-start border-4 border-danger">
                <div class="icon-shape bg-danger bg-opacity-10 text-danger">
                    <i class="bi bi-trophy"></i>
                </div>
                <p class="stat-label mb-1">TOP PRODUCT</p>
                <h3 class="stat-value text-truncate" style="font-size: 1.4rem;">@Model.TopProduct</h3>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-8">
            <div class="glass-card p-4">
                <div class="card-title-premium d-flex justify-content-between align-items-center">
                    <span>Revenue Velocity</span>
                    <i class="bi bi-three-dots-vertical text-muted cursor-pointer"></i>
                </div>
                <div class="chart-container">
                    <canvas id="revenueFlowChart"></canvas>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="glass-card p-4">
                <div class="card-title-premium">Product Affinity</div>
                <div class="chart-container">
                    <canvas id="productMixChart"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    // Universal Chart Settings
    Chart.defaults.font.family = "'Plus Jakarta Sans', sans-serif";
    Chart.defaults.color = '#64748b';

    // 1. Revenue Velocity (Line Chart with Gradient)
    const revCtx = document.getElementById('revenueFlowChart').getContext('2d');
    const purpleGradiant = revCtx.createLinearGradient(0, 0, 0, 400);
    purpleGradiant.addColorStop(0, 'rgba(99, 102, 241, 0.2)');
    purpleGradiant.addColorStop(1, 'rgba(99, 102, 241, 0)');

    new Chart(revCtx, {
        type: 'line',
        data: {
            labels: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.MonthlyTrends.Select(m => m.Month))),
            datasets: [{
                label: 'Revenue',
                data: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.MonthlyTrends.Select(m => m.Revenue))),
                borderColor: '#6366f1',
                borderWidth: 4,
                tension: 0.4,
                fill: true,
                backgroundColor: purpleGradiant,
                pointBackgroundColor: '#fff',
                pointBorderColor: '#6366f1',
                pointBorderWidth: 2,
                pointRadius: 6,
                pointHoverRadius: 8
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
                x: { grid: { display: false }, border: { display: false } },
                y: { grid: { color: '#f1f5f9' }, border: { display: false } }
            }
        }
    });

    // 2. Product Affinity (Modern Polar Area or Bar)
    const prodCtx = document.getElementById('productMixChart').getContext('2d');
    new Chart(prodCtx, {
        type: 'bar',
        data: {
            labels: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.SalesByProduct.Select(s => s.ProductName))),
            datasets: [{
                data: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.SalesByProduct.Select(s => s.TotalRevenue))),
                backgroundColor: '#6366f1',
                borderRadius: 8,
                barThickness: 15
            }]
        },
        options: {
            indexAxis: 'y',
            responsive: true,
            maintainAspectRatio: false,
            plugins: { legend: { display: false } },
            scales: {
                x: { grid: { display: false }, display: false },
                y: { grid: { display: false }, border: { display: false } }
            }
        }
    });
</script> *@